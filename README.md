# Mini-Pic

Download parts of diffusiondb.

1. Run cargo run --bin train_tokenizer --release to create tokenizer.json
2. Run cargo run --bin preprocess --release to prepare 64×64 images
3. Start training!
